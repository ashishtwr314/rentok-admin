# Supabase Configuration
# Get these values from your Supabase project dashboard
# Go to Settings > API to find your project URL and anon key

NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Storage Configuration (Legacy - will be replaced by ImageKit)
NEXT_PUBLIC_SUPABASE_STORAGE_BUCKET=rentok-assets

# ImageKit Configuration
# Get these values from your ImageKit dashboard (https://imagekit.io/dashboard)
NEXT_PUBLIC_IMAGEKIT_PUBLIC_KEY=your-imagekit-public-key
NEXT_PUBLIC_IMAGEKIT_URL_ENDPOINT=https://ik.imagekit.io/your-imagekit-id
IMAGEKIT_PRIVATE_KEY=your-imagekit-private-key

# SMTP Email Configuration
# Configure your SMTP settings for sending emails
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=rentok.studio@gmail.com
SMTP_PASS=your-app-password-here
SMTP_FROM_NAME=Rent.OK
SMTP_FROM_EMAIL=rentok.studio@gmail.com

# Instructions:
# 1. Copy this file to .env.local
# 2. Replace the values above with your actual Supabase credentials
#    - SUPABASE_SERVICE_ROLE_KEY: Go to Settings > API > service_role key (Secret!)
#    - This is required for admin operations like updating auth users
# 3. Replace the ImageKit values with your actual ImageKit credentials
# 4. Replace SMTP values with your email server credentials
#    - For Gmail: Use App Password (not your regular password)
#    - Go to Google Account > Security > 2-Step Verification > App passwords
# 5. Make sure to enable Phone authentication in your Supabase project
# 6. Create an ImageKit account and get your credentials from the dashboard
# 
# IMPORTANT: Never commit .env.local to git! Keep your secrets safe!
